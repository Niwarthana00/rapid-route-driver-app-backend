# Server Configuration
PORT=5000
NODE_ENV=development

# PostgreSQL Database Configuration (Shared with Passenger Backend)
DB_HOST=localhost
DB_PORT=5433
DB_USER=postgres
DB_PASSWORD=dev_password_only
DB_NAME=bus_enterprise
DATABASE_URL=postgresql://postgres:dev_password_only@localhost:5433/bus_enterprise

# JWT Secret
JWT_SECRET=super_secret_rapid_route_jwt_key_2026
JWT_EXPIRES_IN=30d

# CORS Origin
CORS_ORIGIN=*
